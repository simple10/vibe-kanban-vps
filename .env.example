# Agent API keys
# For Claude: either set ANTHROPIC_API_KEY or use OAuth login via claude-login.sh
# ANTHROPIC_API_KEY=sk-ant-...
# GOOGLE_API_KEY=
# OPENAI_API_KEY=

# Cloudflare Tunnel token (required)
# Create at: Cloudflare Zero Trust dashboard → Networks → Tunnels → Create
# Configure the tunnel's public hostname to point to http://vibe-kanban:3000
# Auth is handled via Cloudflare Access policies (configured in the CF dashboard)
CF_TUNNEL_TOKEN=

# Domain served by the tunnel (optional - used for Cloudflare Access verification)
# Comment out VK_DOMAIN if you're not using a domain with your Cloudflare Tunnel
VK_DOMAIN="vibekanban.<YOUR-DOMAIN>.com"

# VPS SSH connection (used by Claude to deploy via scp/ssh)
VPS_IP=
SSH_KEY_PATH=~/.ssh/vps1_vibekanban_ed25519
SSH_USER=root
SSH_PORT=22

# Install directory on the VPS (default: /home/vibe-kanban)
# INSTALL_DIR=/home/vibe-kanban

# Container resource limits (vibe-kanban)
# Adjust based on expected concurrent agents. Each coding agent uses ~500MB RAM.
# VK_CPU_LIMIT=4        # max CPU cores
# VK_MEM_LIMIT=8g       # max memory (e.g. 4g, 8g, 16g)
# VK_CPU_RESERVE=1      # guaranteed CPU cores
# VK_MEM_RESERVE=2g     # guaranteed memory

# IDE Remote SSH (VS Code / Cursor) — connect your editor directly into the container
VK_IDE_SSH=true          # Enable sshd inside the container (default: false)
VK_SSH_PORT=2222         # Host port mapped to container sshd (default: 2222)

# Optional
# RUST_LOG=info

# Github User
# The github-login.sh helper script attempts to auto set user details in
# vibe-kanban if these values are left empty.
# Run `gh api user` and `gh api user/emails` to fetch your Github user details before
# deploy if you want to ensure specific user details.
# GIT_AUTHOR_NAME="Your Name"
# GIT_AUTHOR_EMAIL="you@example.com"
